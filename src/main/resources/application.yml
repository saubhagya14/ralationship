server:
  port: 1200
spring:
  application:
    name: demo

  datasource:
    url: jdbc:mysql://localhost:3306/relationship
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  logger:
    file-name: myApp

